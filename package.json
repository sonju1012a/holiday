{
  "name": "charye-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "교육용 3D 차례상 차리기 게임",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:toss": "vite build --mode toss",
    "preview:toss": "vite build --mode toss && vite preview",
    "dev:toss": "vite --mode toss --port 5198 --strictPort",
    "screenshots": "node scripts/screenshots.mjs"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^3.2.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "playwright-core": "^1.47.2",
    "vite": "^6.0.0"
  }
}
